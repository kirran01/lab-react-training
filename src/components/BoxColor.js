import React from 'react';

const BoxColor = (props) => {
    return (
        <div>
            <p>r={props.r}</p>
            <p>g={props.g}</p>
            <p>b={props.b}</p>
        </div>
    );
}

export default BoxColor;
